## Purpose

{{purpose}}

## Key Workflows

{{workflows}}

## bt CLI Reference (Generated from README)

{{readme_excerpt}}

## Reference Strategy

- For command syntax, use `bt --help` and `<subcommand> --help` (for example `bt sql --help`, `bt view --help`).
- Prefer prefetched local docs in `skills/docs/README.md`, then section indexes like `skills/docs/evaluate/_index.md` and `skills/docs/reference/_index.md`.
- SQL reference is available at `skills/docs/reference/sql.md`.
- If local docs are missing or stale, run `bt docs fetch` (optionally with `--refresh`).
