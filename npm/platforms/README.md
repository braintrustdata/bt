# Braintrust CLI platform binary

This package contains a prebuilt `bt` binary for the operating system and architecture specified in its `package.json`.
It is installed automatically as an optional dependency of [`@braintrust/bt`](https://www.npmjs.com/package/@braintrust/bt), which exposes the `bt` command.
Install `@braintrust/bt` instead of installing this package directly.
